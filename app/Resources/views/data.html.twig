<!DOCTYPE html>
<html lang="pl">
    <head>
        <title>Encje</title>
        <meta charset="UTF-8" />
    </head>
    <body>
       <a href="/add">Dodaj</a> 
      <h1>Rodzice</h1>
      {% for f in parents %}
          <p>ID: {{ f.id }}</p>
          <p>Imię: {{ f.name }}</p>
          <p>Nazwisko: {{ f.surname }}</p>
      {% endfor %}
      <h1>Dzieci</h1>
      {% for c in childrens %}
          <p>ID: {{ c.id }}</p>
          <p>Imię: {{ c.name }}</p>
          <p>Nazwisko: {{ c.surname }}</p>
          {% for i in c.parent %}
              <p> i.id </p>a
          {% endfor %}
      {% endfor %}

    </body>
</html>
